# local lakehouse data — generated, never committed. The folder itself is kept
# (via .gitkeep) so the bind-mount target exists in the repo.
lakehouse/*
!lakehouse/.gitkeep

# python
__pycache__/
*.pyc
.venv/
venv/
.env

# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/
.user.yml

# spark
spark-warehouse/
metastore_db/
derby.log

# os / editor
.DS_Store
.idea/
.vscode/
