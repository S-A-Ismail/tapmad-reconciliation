# Dependencies for the pipeline + dbt image (Spark runs in local[*] mode inside
# the container, so no separate cluster is needed). Airflow is intentionally
# NOT here - it lives in its own image to avoid dependency clashes.

pyspark==3.5.1
delta-spark==3.2.0

# dbt: spark[session] lets dbt run against the in-container Spark with no
# Thrift server; databricks is the real-warehouse target.
dbt-core==1.8.6
dbt-spark[session]==1.8.0
dbt-databricks==1.8.5

# synthetic data
Faker==25.0.0
