# diagram images

`erd.png` — the entity-relationship diagram, exported from Lucidchart.

To (re)export: open the Lucid document, select the **ERD** page, and
**File → Export → PNG**, saving it here as `erd.png`. The README embeds it via
`docs/img/erd.png`.
